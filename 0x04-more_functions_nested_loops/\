#include "main.h"

/**
 * main - write the function that checks for the uppercase character.
 *@x: input
 * Return: 1 for  upper or 0 for any other 
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
