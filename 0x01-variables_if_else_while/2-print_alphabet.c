#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main() {
    // Print the alphabet in lowercase
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // Print a new line
    putchar('\n');

    return 0;
}
